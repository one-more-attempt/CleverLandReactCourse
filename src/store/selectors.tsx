import { RootState } from ".";

export const randomCharSliceData = (state: RootState) =>
  state.randomCharSliceReducer;

export const characterTableSliceData = (state: RootState) =>
  state.characterTableSliceReducer;

  export const selectedCharacterSliceData = (state: RootState) =>
  state.selectedCharacterSliceReducer;
  
